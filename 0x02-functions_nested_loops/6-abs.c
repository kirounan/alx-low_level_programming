#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs - Check the value of c
 * @c: The number to be checked
 * Return: the number absolute
 */
int _abs(int c)
{
	return (abs(c));
}
